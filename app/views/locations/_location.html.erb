<li>
  <% unless location.images.first.nil? %>
    <%= image_tag location.images.first.url.to_s,
      size: Settings.locations.image.size %>
  <% end %>
  <%= link_to location.name, location_path(location) %>
  <% if can? [:update, :destroy], Location %>
    | <%= link_to t(".edit"), edit_location_path(location) %>
    | <%= link_to t(".delete"), location, method: :delete,
      data: {confirm: t(".sure")} %>
  <% end %>
</li>
